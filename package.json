{
  "name": "majibooks",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "client:build": "npm run build --workspace=@majibooks/client",
    "client:dev": "npm run dev --workspace=@majibooks/client",
    "client:npm-i": "npm i --workspace=@majibooks/client",
    "server:build": "npm run build --workspace=@majibooks/server",
    "server:dev": "npm run dev --workspace=@majibooks/server",
    "server:npm-i": "npm i --workspace=@majibooks/server",
    "server:test": "npm run test --workspace=@majibooks/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
